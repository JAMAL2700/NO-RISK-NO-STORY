# Backup-Exports (enthalten persönliche Trade-Daten, gehören nicht ins Repo)
trading-journal-backup-*.json
*.backup.json

# OS/Editor-Kram
.DS_Store
Thumbs.db

# Node-Dependencies fuer scripts/fetch-btc-news.mjs (installiert die GitHub Action selbst)
node_modules/
